---
title: 'State machines'
---

- What is a state machine?
- How are state machines useful?

## Creating a state machine

- `createMachine({ ... })`

## Interpreting a state machine

- Think of machine as the blueprint, the actor as the instance
- `interpret(machine).start()`
- See [actors](#)

TODO: example with subscribing to intepreted machine

## Providing implementations

- `machine.provide({ ... })`

## Specifying types

- `createMachine({ types })`
- Do not use generics!

## Transition function

- `machine.transition(state, event)`
- Returns next state
- Good for testing or building a custom interpreter

## State nodes

- `machine.getStateNodeById(stateId)`

## API

Link to API

## TypeScript

## Cheatsheet

## Resources
