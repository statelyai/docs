---
deps:
  ["options-intro", "guards-actions-services-delays-intro"]
group: basics
status: complete # in-progress, complete
needs: # content, tech-review, copy-review
---

You can specify machine options in several places. Firstly, you can set options inside the machine itself:

```ts twoslash
import { createMachine } from "xstate";

const machine = createMachine(
  {
    // Set config here
  },
  {
    // Set options here:
    actions: {},
    services: {},
    guards: {},
    delays: {},
  },
);
```

Or, you can specify options later with a `.withConfig` call:

```ts twoslash
import { createMachine } from "xstate";

const machine = createMachine({});

// ---cut---

machine.withConfig({
  actions: {},
  services: {},
  guards: {},
  delays: {},
});
```
